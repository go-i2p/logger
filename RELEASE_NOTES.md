Release notes for: `logger` Version `0.1.52`
==============================================

This release establishes the RELEASE_NOTES.md file for the logger package.
`go-i2p/logger` is just some packaged configuration around logrus.
It ensures that we use uniform logging across all systems.
It does basically nothing else.
This package is not going to change much and is probably stable.